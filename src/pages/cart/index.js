import React from 'react';

import { Container, TextContext } from './styles';

export default function Cart() {
    return (
        <Container>
            <TextContext>CARRINHO</TextContext>
        </Container>
    );
}
